# BMS 2025 - תהליך עבודה ופיתוח
## Development & Deployment Workflow

---

## 🎯 מבנה הסביבות

```
┌────────────────────────────────────────────┐
│          DEVELOPMENT (פיתוח)               │
│  Branch: development                       │
│  Port: localhost:3000                      │
│  Database: Supabase DEV                    │
│  כאן מפתחים תכונות חדשות ובודקים           │
└──────────────┬─────────────────────────────┘
               │
               │ אישור + בדיקה
               ↓
┌────────────────────────────────────────────┐
│          STAGING (בדיקות)                  │
│  Branch: staging                           │
│  URL: Preview URL                          │
│  Database: Supabase STAGING                │
│  בדיקה אחרונה לפני שחרור                   │
└──────────────┬─────────────────────────────┘
               │
               │ אישור סופי + גיבוי
               ↓
┌────────────────────────────────────────────┐
│          PRODUCTION (ייצור)                │
│  Branch: main                              │
│  URL: keshet-system.vercel.app             │
│  Database: Supabase PRODUCTION             │
│  המשתמשים האמיתיים עובדים כאן              │
└────────────────────────────────────────────┘
```

---

## 📋 תהליך פיתוח יומיומי

### 1️⃣ התחלת יום עבודה (פיתוח)

```bash
# עבור ל-branch של פיתוח
git checkout development

# משוך עדכונים אחרונים
git pull origin development

# הרץ בסביבת פיתוח
start-dev.bat
```

**או פשוט לחץ פעמיים על:** `start-dev.bat`

---

### 2️⃣ פיתוח תכונה חדשה

#### א. צור branch חדש לתכונה
```bash
git checkout -b feature/תיאור-התכונה
```

לדוגמה:
```bash
git checkout -b feature/customer-advanced-search
git checkout -b feature/whatsapp-integration
git checkout -b fix/order-calculation-bug
```

#### ב. פתח והתחל לעבוד
1. **הרץ**: `start-dev.bat`
2. **פתח דפדפן**: http://localhost:3000
3. **עבוד על הקוד** - שנה, בדוק, תקן
4. **שמור שינויים** (Ctrl+S)
5. **הדפדפן יתעדכן אוטומטית** (Hot Reload)

#### ג. בדוק שהכל עובד
- ✅ אין שגיאות בקונסול
- ✅ התכונה עובדת כמצופה
- ✅ לא שברת דברים אחרים

---

### 3️⃣ שמירה ל-Git (נקודת שחזור)

```bash
# הצג מה השתנה
git status

# הוסף את כל הקבצים שהשתנו
git add .

# צור commit עם תיאור ברור
git commit -m "תיאור קצר של מה עשית"
```

**דוגמאות טובות לcommit:**
```bash
git commit -m "Add customer search modal with smart filtering"
git commit -m "Fix VAT calculation in orders"
git commit -m "Update database schema for new fields"
```

---

### 4️⃣ העלאה ל-GitHub (גיבוי)

```bash
# דחוף את השינויים ל-GitHub
git push origin feature/שם-התכונה
```

**זה יוצר גיבוי ב-GitHub!** עכשיו השינויים שלך מוגנים.

---

### 5️⃣ מיזוג ל-Development (אחרי אישור)

```bash
# חזור ל-development branch
git checkout development

# מזג את התכונה החדשה
git merge feature/שם-התכונה

# העלה ל-GitHub
git push origin development
```

---

## 🚀 העלאה ל-Production (משתמשים אמיתיים)

### ⚠️ חשוב מאוד - תהליך זהיר!

#### שלב 1: גיבוי בסיס נתונים (חובה!)

```bash
# הרץ את סקריפט הגיבוי
backup.bat
```

זה יוצר קובץ בתיקייה `backups/` עם תאריך ושעה.

**שמור את הגיבוי במקום בטוח!**

---

#### שלב 2: בדיקה אחרונה

1. **בדוק שהכל עובד ב-development**
2. **בדוק שאין שגיאות בקונסול**
3. **בדוק שהסקריפט SQL רץ (אם יש שינויים בDB)**
4. **תעד את השינויים** - מה חדש? מה השתנה?

---

#### שלב 3: מיזוג ל-Main

```bash
# חזור ל-main branch
git checkout main

# משוך את הגרסה העדכנית ביותר
git pull origin main

# מזג את development ל-main
git merge development

# העלה ל-GitHub
git push origin main
```

---

#### שלב 4: Build Production

```bash
# בנה גרסת production
build-prod.bat
```

זה יוצר תיקייה `build/` עם הקבצים המוכנים לפרסום.

---

#### שלב 5: העלאה לשרת

**אופציה 1: Vercel (מומלץ)**
1. התחבר ל-[Vercel](https://vercel.com)
2. Import Project מ-GitHub
3. בחר את ה-repository
4. הגדר את משתני הסביבה:
   - `REACT_APP_SUPABASE_URL`
   - `REACT_APP_SUPABASE_KEY`
5. Deploy!

**אופציה 2: Netlify**
1. התחבר ל-[Netlify](https://netlify.com)
2. New site from Git
3. בחר GitHub repository
4. הגדר Build settings:
   - Build command: `npm run build`
   - Publish directory: `build`
5. הוסף Environment variables
6. Deploy!

---

## 🔧 קבצים חשובים

### קבצי סביבה
- `.env.development` - הגדרות פיתוח
- `.env.production` - הגדרות ייצור (משתמשים אמיתיים)
- `.env` - הקובץ הפעיל (מועתק אוטומטית)

### סקריפטים
- `start-dev.bat` - הרצה בסביבת פיתוח
- `build-prod.bat` - בנייה לייצור
- `backup.bat` - גיבוי בסיס נתונים
- `start.bat` - הרצה רגילה (ישן - אל תשתמש)

### תיקיות
- `src/` - קוד המקור
- `build/` - קבצים מוכנים לפרסום (נוצרים אוטומטית)
- `backups/` - גיבויי בסיס נתונים
- `scripts/` - סקריפטים עזר

---

## 📝 דוגמת תהליך מלא

### תרחיש: הוספת תכונת "מחיקת לקוח"

```bash
# 1. צור branch חדש
git checkout development
git checkout -b feature/delete-customer

# 2. הרץ בסביבת פיתוח
start-dev.bat

# 3. עבוד על הקוד
# - הוסף כפתור מחיקה ב-Customers.jsx
# - הוסף פונקציה deleteCustomer
# - בדוק שהכל עובד

# 4. שמור ל-Git
git add .
git commit -m "Add delete customer functionality with confirmation modal"

# 5. העלה ל-GitHub (גיבוי)
git push origin feature/delete-customer

# 6. מזג ל-development
git checkout development
git merge feature/delete-customer
git push origin development

# 7. בדוק ב-development שהכל תקין
# המתן יום-יומיים, בדוק שאין באגים

# 8. לפני העלאה ל-production - גיבוי!
backup.bat

# 9. מיזוג ל-production
git checkout main
git pull origin main
git merge development
git push origin main

# 10. Deploy אוטומטי (אם הגדרת Vercel/Netlify)
# או build ידני:
build-prod.bat
# ואז העלה את תיקיית build/ לשרת
```

---

## ⚡ קיצורי מקלדת ושימושים נפוצים

### פקודות Git חשובות
```bash
# ראה סטטוס נוכחי
git status

# ראה היסטוריית commits
git log --oneline

# בטל שינויים שעשית (לפני commit)
git checkout .

# חזור לcommit קודם
git reset --hard <commit-id>

# ראה הבדלים מה השתנה
git diff

# החלף בין branches
git checkout <branch-name>

# ראה את כל ה-branches
git branch -a
```

### פקודות NPM חשובות
```bash
# התקן תלויות (אחרי clone)
npm install

# הרץ בסביבת פיתוח
npm start

# בנה לייצור
npm run build

# בדוק תקינות
npm test
```

---

## 🔒 כללי אבטחה

### ❌ אל תעשה:
- לא לשתף את `.env` בגיטהאב
- לא לעשות `git push --force` ל-main
- לא לשנות ישירות ב-production ללא גיבוי
- לא למחוק את תיקיית `backups/`

### ✅ תמיד עשה:
- גיבוי לפני שינויים ב-production
- בדיקה ב-development לפני העלאה
- commit עם הודעות ברורות
- pull לפני push (למנוע קונפליקטים)

---

## 🆘 פתרון בעיות נפוצות

### בעיה: "Cannot find module"
```bash
npm install
```

### בעיה: "Port 3000 already in use"
```bash
# חפש את התהליך
netstat -ano | findstr :3000

# סגור את התהליך
taskkill /F /PID <מספר_תהליך>
```

### בעיה: שינויים לא מופיעים בדפדפן
1. Hard Refresh: `Ctrl + Shift + R`
2. מחק cache: `Ctrl + Shift + Delete`
3. נקה node cache: `rm -rf node_modules/.cache`

### בעיה: שגיאות בבסיס נתונים
1. בדוק שהרצת את `UPDATE_SCHEMA_FOR_ORDERS.sql`
2. בדוק את ההרשאות ב-Supabase
3. בדוק את ה-Service Role Key ב-`.env`

---

## 📞 עזרה ותמיכה

אם משהו לא עובד:
1. **בדוק את הקונסול בדפדפן** (F12)
2. **בדוק את לוג השרת** (Terminal)
3. **חפש שגיאות דומות ב-Google**
4. **צור Issue ב-GitHub** עם פרטי השגיאה
5. **שאל את Claude Code** 😊

---

## 🎓 לסיכום

### סדר עבודה מומלץ:

```
1. start-dev.bat          → פתח סביבת פיתוח
2. עבוד על הקוד            → פתח ושנה
3. git add + commit       → שמור נקודת שחזור
4. git push               → גיבוי ב-GitHub
5. בדוק שהכל עובד          → test test test
6. backup.bat             → גבה את הDB
7. git merge to main      → העלה ל-production
8. build-prod.bat         → בנה גרסת ייצור
9. Deploy                 → פרסם למשתמשים
```

**זכור: תמיד יותר טוב לעשות גיבוי מיותר מאשר לאבד מידע!**

---

<p align="center">
  <strong>BMS 2025 - Business Management System</strong><br>
  Made with ❤️ by Claude Code
</p>
